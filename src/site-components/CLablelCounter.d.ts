import * as React from "react";
import * as Types from "./webflow_modules/types";

declare function CLablelCounter(props: {
  count?: React.ReactNode;
  label?: React.ReactNode;
}): React.JSX.Element;
