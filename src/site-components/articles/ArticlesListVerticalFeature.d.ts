import * as React from "react";
import * as Types from "../webflow_modules/types";

declare function ArticlesListVerticalFeature(props: {
  slotCardMeta?: React.ReactNode;
  slotReadMore?: React.ReactNode;
}): React.JSX.Element;
