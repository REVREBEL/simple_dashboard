import * as React from "react";
import * as Types from "./webflow_modules/types";

declare function SolutionsGrid(props: {
  slotButton?: React.ReactNode;
  slotLabel?: React.ReactNode;
  text?: React.ReactNode;
  title?: React.ReactNode;
}): React.JSX.Element;
