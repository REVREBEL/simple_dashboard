import * as React from "react";
import * as Types from "../webflow_modules/types";

declare function ContentSlideHeader(props: {
  eyebrowText?: React.ReactNode;
  headlineText?: React.ReactNode;
  subHeadlineText?: React.ReactNode;
}): React.JSX.Element;
